apiVersion: v1
kind: Service
metadata:
  labels:
    app: notification-service
  name: notification-service-tcp
spec:
  ports:
  - name: "tcp"
    port: 5008
    protocol: TCP
    targetPort: 5008
  selector:
    app: notification-service
  type: ClusterIP

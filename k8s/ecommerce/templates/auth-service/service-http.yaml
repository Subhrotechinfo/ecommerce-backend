apiVersion: v1
kind: Service
metadata:
  labels:
    app: auth-service
  name: auth-service-http
spec:
  ports:
  - name: "http"
    port: 5001
    protocol: TCP
    targetPort: 5001
  selector:
    app: auth-service
  type: NodePort


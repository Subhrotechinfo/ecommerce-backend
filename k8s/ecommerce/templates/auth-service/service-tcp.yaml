apiVersion: v1
kind: Service
metadata:
  labels:
    app: auth-service
  name: auth-service-tcp
spec:
  ports:
  - name: "tcp"
    port: 5002
    protocol: TCP
    targetPort: 5002
  selector:
    app: auth-service
  type: ClusterIP


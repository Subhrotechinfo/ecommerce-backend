apiVersion: v1
kind: Service
metadata:
  labels:
    app: orders-service
  name: orders-service-tcp
spec:
  ports:
  - name: "tcp"
    port: 5006
    protocol: TCP
    targetPort: 5006
  selector:
    app: orders-service
  type: ClusterIP

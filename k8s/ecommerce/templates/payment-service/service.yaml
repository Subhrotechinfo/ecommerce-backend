apiVersion: v1
kind: Service
metadata:
  labels:
    app: payments-service
  name: payments-service
spec:
  ports:
  - name: "tcp"
    port: 5004
    protocol: TCP
    targetPort: 5004
  selector:
    app: payments-service
  type: ClusterIP


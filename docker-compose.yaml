services:
  products:
    build:
      context: .
      dockerfile: ./services/products/Dockerfile
      target: runner
    command: pnpm run dev
    env_file:
      - ./services/products/.env
      - .env
    ports:
      - '3000:3000'
    volumes:
      - .:/usr/src/app
  mongo:
    image: mongo
